{
  "name": "haggadah-admin",
  "version": "1.0.0",
  "description": "Haggadah verse management system with admin panel",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "cd react && npm install && npm run build && cd .. && rm -rf public && cp -R react/dist public",
    "build-start": "cd react && npm install && npm run build && cd .. && node server.js"
  },
  "keywords": [
    "haggadah",
    "admin",
    "verse"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@google/genai": "^1.44.0",
    "@supabase/supabase-js": "^2.98.0",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.18.2"
  }
}
